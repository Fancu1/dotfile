---
description: Frontend rules (UI consistency, accessibility, verification)
globs: ["**/*"]
alwaysApply: false
---

## Skill Policy（Auto）
- Must Use: `frontend-design`
- May Use: `webapp-testing`（涉及 UI/交互变更的验收）

## Scope
- Applies when: 前端/UI/交互相关任务

## Rules
- UI 变更必须给出可视验证步骤（MCP/Playwright/截图）。
- 保持组件/样式一致性，不引入无关设计语言。
- 需要新增依赖时先 Ask First。
