---
description: Mobile app rules (platform constraints, build/run, testing)
globs: ["**/*"]
alwaysApply: false
---

## Skill Policy（Auto）
- Must Use: `frontend-design`（移动端 UI/交互与信息架构）
- May Use: `backend-development`（当涉及 API/服务协同、性能与数据流）
- May Use: `systematic-debugging`（构建失败/签名/权限/运行时崩溃定位）

## Scope
- Applies when: iOS/Android/Flutter/React Native 等移动端相关任务

## Rules
- 必须说明平台约束（iOS/Android 版本、权限、证书）。
- 需要真机/模拟器时先 Ask First。
- 提供构建与验证步骤（build/run/test）。
